<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BrahmsLab.WinUi3App.Pages.SpectralScanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrahmsLab.WinUi3App.Pages"
    xmlns:views="using:BrahmsLab.WinUi3App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <views:ScanPageContextView Grid.Row="0" DataContext="{x:Bind ViewModel.ScanPageContext}"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="320"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <views:SpectralScanFormView Grid.Column="0" DataContext="{x:Bind ViewModel.SpectralScanForm}"/>
            <Grid Grid.Column="1" Margin="12,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <views:ScanHistoryView Grid.Row="0" DataContext="{x:Bind ViewModel.ScanHistory}"/>
                <views:SpectralGraphView Grid.Row="1" DataContext="{x:Bind ViewModel.Graph}" Margin="0,12,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
