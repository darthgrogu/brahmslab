<UserControl
    x:Class="BrahmsLab.WinUi3App.Views.SpectralScanFormView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:BrahmsLab.WinUi3App.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:num="using:Windows.Globalization.NumberFormatting"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:SpectralScanFormViewModel, IsDesignTimeCreatable=False}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <UserControl.Resources>
        <num:DecimalFormatter x:Key="IntegerFormatter" FractionDigits="0" IsGrouped="False"/>
    </UserControl.Resources>

    <Grid Padding="24" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="New Spectral Scan" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" ItemsSource="{x:Bind ViewModel.HerbariumCodes}"
                      SelectedItem="{x:Bind ViewModel.SelectedHerbariumCode, Mode=TwoWay}"
                      IsEditable="True" Header="Herbarium Code"/>
            <TextBox Grid.Column="1" Text="{x:Bind ViewModel.UserInputCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Header="User Input Code (Barcode, Accession #)"/>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="Measurement Context" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,5"/>
            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" ItemsSource="{x:Bind ViewModel.TargetClasses}" SelectedItem="{x:Bind ViewModel.SelectedTargetClass, Mode=TwoWay}" Header="Target Class"/>
                <ComboBox Grid.Column="1" ItemsSource="{x:Bind ViewModel.BackgroundClasses}" SelectedItem="{x:Bind ViewModel.SelectedBackgroundClass, Mode=TwoWay}" Header="Background Class"/>
                <ComboBox Grid.Column="2" ItemsSource="{x:Bind ViewModel.DevelopmentalStages}" SelectedItem="{x:Bind ViewModel.SelectedDevelopmentalStage, Mode=TwoWay}" Header="Developmental Stage"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="Measurement Conditions" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,5"/>
            <Grid ColumnSpacing="10" RowSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ToggleSwitch Grid.Row="0" Grid.Column="0" Header="Low Reflectance Background?" IsOn="{x:Bind ViewModel.HasLowReflectanceBackground, Mode=TwoWay}"/>
                <ToggleSwitch Grid.Row="0" Grid.Column="1" Header="Background in Measurement?" IsOn="{x:Bind ViewModel.HasBackgroundInMeasurement, Mode=TwoWay}"/>
                <muxc:NumberBox Grid.Row="1" Grid.Column="1" Value="{x:Bind ViewModel.PercentBackgroundInMeasurement, Mode=TwoWay}"
                                Visibility="{x:Bind ViewModel.HasBackgroundInMeasurement, Mode=OneWay}"
                                Header="Percent Background (%)" SpinButtonPlacementMode="Compact" SmallChange="1"
                                NumberFormatter="{StaticResource IntegerFormatter}" Minimum="0" Maximum="100"/>
                <ComboBox Grid.Row="2" Grid.Column="0" ItemsSource="{x:Bind ViewModel.PresenceOptions}" SelectedItem="{x:Bind ViewModel.SelectedHasGlue, Mode=TwoWay}" Header="Glue Present?"/>
                <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{x:Bind ViewModel.PresenceOptions}" SelectedItem="{x:Bind ViewModel.SelectedHasNonGlueContamination, Mode=TwoWay}" Header="Non-Glue Contamination?"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,12,0,0">
            <TextBlock Text="Measurement Flags" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,5"/>
            <ItemsControl ItemsSource="{x:Bind ViewModel.Flags}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:SelectableFlagViewModel">
                        <ToggleButton Content="{x:Bind DisplayName}" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" Margin="0,0,5,5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}" BorderThickness="1" Margin="0,5,0,0" Padding="5">
                <TextBlock Text="{x:Bind ViewModel.MeasurementFlagsString, Mode=OneWay}"
                           TextWrapping="Wrap"
                           Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"
                           FontStyle="Italic"/>
            </Border>
        </StackPanel>

        <Button Grid.Row="5" Content="Save Scan"
                Command="{x:Bind ViewModel.SaveScanCommand}"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Style="{ThemeResource AccentButtonStyle}"/>
    </Grid>
</UserControl>