<UserControl
    x:Class="BrahmsLab.WinUi3App.Views.DeviceWidgetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:BrahmsLab.WinUi3App.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:DeviceWidgetViewModel, IsDesignTimeCreatable=True}">

    <Grid ColumnSpacing="12" Padding="12" Height="80" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" 
               Source="/Assets/micronir.png" 
               Height="64"
               Stretch="Uniform"
               VerticalAlignment="Center"/>

        <Ellipse Grid.Column="1" Width="12" Height="12" Fill="Green" VerticalAlignment="Center"/>

        <StackPanel Grid.Column="2" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind ViewModel.DeviceName}" FontWeight="SemiBold"/>
            <TextBlock Text="{x:Bind ViewModel.DeviceStatus}" FontSize="12" Opacity="0.8"/>
        </StackPanel>

        <Button Grid.Column="3" Content="&#xE713;" FontFamily="Segoe Fluent Icons"
                IsEnabled="False"/>
    </Grid>
</UserControl>